# S1 诊断检查清单 v2

2026-09-17 EDT 用户修订。覆盖失败报告里“专家回放后直接训特权 state”的顺序。按依赖就绪立即执行，不等待某个星期。原 S1 0/10 保留，S2/S3 未放行。

## 证据确认与解释边界

采集侧不是从评估脚本推测的：官方 `pick/013_apple.json` 的 `env_info.env_kwargs` 写明 `shader_dir="minimal"`、`sim_backend="gpu"`、`num_envs=252`。配套 JSON SHA256 为 `4e9cc313ac816f73131eb2ab8b59156fcac864eae1b4509bd549b2cd3ca78736`。当前 ManiSkill `sapien_env.py:210–214` 将旧 `shader_dir` 映射到传感器、展示相机和 viewer 的 shader 配置。已保存 [采集配置证据](results/s1-render-alignment-2026-09-18-run01/source-render-contract.json)。这确认记录的采集配置；原始 252 环境采集程序的运行二进制未重建。

四个父轨迹的边界核查与用户表一致。父 0/17 在 grasp 首帧，父 20/20 在 reach 最后一帧；两者动作块均没有时间 padding。它们不足以证明所有失败都只集中在闭爪，但足以把该边界列为重点诊断对象。

完整数据统计见 [phase-and-padding-counts.json](results/s1-render-alignment-2026-09-18-run01/phase-and-padding-counts.json)：

| 划分 | 父轨迹 / 当前帧 | reach | grasp | move | 含时间 padding 的当前帧 |
|---|---:|---:|---:|---:|---:|
| train | 150 / 6835 | 2676 | 552（8.08%） | 3607 | 1350（19.75%） |
| validation | 50 / 2033 | 914 | 187（9.20%） | 932 | 450（22.13%） |

训练轨迹长度中位数 41、最短 21；验证中位数 40、最短 22。训练 target slots 中 6750/68350（9.88%）是末端重复，须与“19.75% 当前帧的块含 padding”区分。

需要保留三点限制：

- 一 epoch 中每个**当前观测**只采样一次，但 horizon10 的重叠动作块会让边界动作作为未来 target 出现最多十次；不能说闭爪动作标签总共只监督一次。
- 两帧的目标块没有 padding，不能以直接的末端补齐解释这两帧反向；全局训练权重是否受到重复尾部监督影响，仍是待验证假说。
- batch1 与域差都是已确认设置，但其对 0/10 的因果贡献尚未确认。原成绩是 CPU/default 条件下的有效失败记录，不删除，也不改称 GPU/minimal 的成绩。

## 1. 冻结模型，先做域对齐对照（已启动）

- 同一 best/6000、归一化、指令、模型随机数序列、十 seed、每集200动作和原生判据；只采用新的 GPU/minimal 环境条件。
- 从原面板恢复保存的全部 simulator/controller 叶子并逐项比较，容差 `1e-5` 在启动前固定；恢复不通过记基础设施失败，停止，不假装起点配对。
- 用保存的 reset_info 读取观测，不额外调用会扣 horizon 的 evaluate。保存恢复后的状态、模型输入、动作和图像。
- 这是 **渲染＋物理后端联合对照**，不能单独归因为 shader。序列化状态一致也不代表两个物理后端的隐藏接触缓存或数值积分完全相同。
- 仅lab GPU1；内部3600秒/外部3660秒，API0、新租机0。当前目录 `runs/s1-native-render-aligned-2026-09-18-run01`，启动外层PID1481028。GPU同时只跑此面板，不并发训练或其他渲染。
- 首个起点实际恢复误差最大1.79e-7，已进入在线执行；完整结果未出，不能声明域差已解释失败。
- 若联合对照有明显改善，优先考虑后续只改shader的控制臂以分离因素；它不是自动授权增加GPU预算。

## 2. 修正时间 padding 监督（待实现及验收）

- 保留原 unmasked S1 基线及作者代码版本，建立独立 masked 训练变体，不改写历史成绩。
- 将真实 `actions_is_pad` 从 LeRobot 贯通到 loss；逐项验证与每条导出轨迹的有效长度相符，不能只增加字段而不使用。
- 对每个样本只在有效时点归一化损失，再对样本取平均；终点不合成“全零动作成功帧”。
- 保持作者 flow 目标和内部32维动作均值不变，本次只屏蔽时间padding。真实13维误差作为独立评估指标，避免一次同时改变时间与通道加权。
- 必测：改动被mask的目标不改变其直接监督项；有效目标改变会改变loss；首帧/末帧/短轨迹、实际loader mask与梯度路径均检查。注意动作token之间仍可能有attention耦合，不能把loss屏蔽等同于彻底移除padding输入。
- masked validation loss 与旧0.1241不直接比较；新检查点仍只由固定留出规则选择。

## 3. microbatch1 / accumulation8（待实现及验收）

- 本次选择同一150训练/50验证父轨迹、同一6835样本/1epoch；不同时再增至2epoch。
- 6835 = 854×8 + 3：保留最后3帧，实际 **855次优化器更新**，最后一次有效batch3，不丢样本、不复制样本凑8。
- 每微批有独立可追溯随机数；累计梯度按实际样本数取均值，再做一次clip/Adam更新。不得只改配置文字或每微批仍更新Adam。
- 学习率时间轴须在启动前冻结：沿用原始按样本进度的schedule，在优化器更新时按累计样本数求值，避免把旧20更新warmup无意变成160帧warmup。日志同时报processed_samples、optimizer_updates、microbatch和实际effective_batch。
- CPU小模型测试对照一次真正batch8的平均梯度/优化器更新，并验证尾部batch3和恢复计数。随后GPU小门必须登记预算，门未过不长训。

## 4. 全验证集 per-phase 冻结推理（待实现）

- 对旧固定检查点和后续留出选定的新检查点使用同一2033个validation当前帧、同一manifest、同一逐帧随机数，覆盖全部50条验证父轨迹；不是只抽八帧。
- 输出 reach/grasp/move 的帧数、首动作13维MSE、有效动作块MSE、夹爪误差和方向不一致率。单列 reach→grasp 边界前后各2帧；夹爪接近零时单列，不随结果调阈值。
- 使用原生夹爪通道7，明确区分原始预测与实际clip/head-mask后的误差。失败尾不伪造完成。
- phase标签只用于离线分组，不输入S1策略，不改变训练采样分布；如果后续要过采样grasp，另开变体。
- 这组指标用于诊断，不用于从多检查点中按测试表现择优；需要提前固定GPU时限并计入S1剩余预算。

## 5. 一次修改后的忠实臂训练与评估（前置门通过后）

- 从相同官方pi05_base初始化，保留相同LoRA位置/秩、父轨迹、state24、图像、动作和归一化；不从本轮best6000继续训练，否则样本暴露量不可比。
- 唯一新增训练因素为时间mask与梯度累计；整体是组合修复对照，不能分别声称二者的独立收益。
- 最多6835样本、855优化器更新、18000秒内部训练上限；启动前核算S1累计18小时和GPU剩余窗口，不自动用满预算。checkpoint按留出masked loss选。
- 完成后GPU/minimal固定十起点评估，与冻结旧模型的域对齐结果比较；成功、失败、未运行分开，保留全部计划回合。
- 若仍不足3/10，继续停在S1。若仍0/10，再启动一次特权state诊断；不在这些变量未处理前运行它。

## 6. 特权臂与C线

特权臂代码仅作CPU草案保存，尚未训练。H5实际含agent24+extra18=42维，缺少完整state51的底盘速度/位置字段；不能把42维版本称为完整官方state51。未来启动前单独冻结该差异及输入契约。

C线可做CPU runner/数据协议准备；GPU工作继续串行。SAC不是VLA，不能用C线通过代替S1或TAPT通过。DROID/GRPO差异、原V8/AC冻结规则保持。

本批专家动作回放在任务/init重建校验失败时退出，0个动作、31.82秒，无策略结果；[失败证据](results/s1-render-alignment-2026-09-18-run01/preceding-expert-replay-failed.json)保留，暂不重跑。

## 最新覆盖：调用对齐补充实验

用户指出任务级S1没有使用窗口指令，代码核实成立。下一训练优先为独立S1-IA：单共享LoRA、窗口z、调用内动作chunk及有效mask，不加族残差/进度头。旧0/10保留，旧任务级3/10不再作为阻止IA实验的理由；新S2仍需新调用级证据门，不能直接解锁。评估先用独立留出调用起点，oracle串接仅诊断。详见 `docs/s1-ia-sft-revision.md`。修复mask/accum8后同时引入IA只能称组合修复，不能声称单因素收益。预算不增加。
