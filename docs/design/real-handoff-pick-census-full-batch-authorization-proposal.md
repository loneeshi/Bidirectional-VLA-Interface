# 真实导航交接 SAC Pick：完整批次续跑申请

**状态：用户于 2026-09-25 回复“授权”，批准下述完整批次范围；批次已执行，见[结果报告](../../research/c2/diagnostics/2026-09-25-real-handoff-pick-census-result.md)。** [前 12 计划预检](../../research/c2/diagnostics/2026-09-25-real-handoff-pick-first12-amended.md)已在实验室 GPU1 完成，按已批准的[复跑门修订](real-handoff-pick-census-replay-amendment-proposal.md)统计。此申请仅覆盖固定清单剩余计划的位置 12–119，不重抽、重排或加入 seed4／8／9 等手挑起点。

## 拟申请的执行范围

| 项目 | 上限与规则 |
|---|---|
| 目标 | 连同已有 10 个有效交接，达到总计 60 个有效原生 Navigate→Pick 交接即停；若 120 计划用尽或时间上限先到，按实数报告。40 个是目标下界，不承诺在时间上限内一定达到。 |
| 计划顺序 | 原 120 计划清单位置 12–119，按序执行；位置 0–11 的原始结果和回执原样复用。 |
| 策略与判据 | 官方 PPO Navigate 500 步、官方逐物体 SAC Pick 200 步、官方严格成功；首次自然 Pick 为基线。所有自然失败只复跑一次，记录结果/步数/机制差异；不稳定失败单列。 |
| 实验室资源 | 仅 GPU1，**本完整批次新增 GPU1 进程时间最多 7200 秒**，单进程最多 600 秒，串行执行；每条完成回执计入累计守卫。既有首段的 915.1961 秒不转入新额度。 |
| 付费服务 | 付费模型/API 请求 0；不使用 GPU0、RunPod 或其它租用资源；不训练。实验室的单价、币种、账单和对应货币金额当前未知，实际用量与任何账单后续核对，不能将未知写为零。 |
| 停止门 | 冻结计划/episode/checkpoint 身份不符、接触计量缺失、正式评测条件改变、单进程或累计授权上限、无法判定的中断回执，立即停止。快照复跑不一致按已批准修订单列，不单独终止采样。 |
| 统计边界 | 自然基线成功率/失败分类与稳定复跑子集分别给分母和 Wilson 区间。若不稳定或未归类占自然失败超过三分之一，或可重复失败少于 10，不用多数类作机制方向判断。人工盲审结果缺失时一致率标缺失。 |

前 12 个计划新增 10 个有效交接，完成最终批次回执用时 517.558 秒，其中包含复用位置 0、1 的 121.813 秒；每计划平均约 43.13 秒。按相同取得率，达到 60 个有效交接还需约 60 个计划、约 2590 秒进程时间。7200 秒是硬上限，用于覆盖导航 500 步、失败复跑、场景切换与吞吐波动；它不是预计耗尽的时间，也不是货币成本估计。

续跑代码候选包 `repo/Bidirectional-VLA-Interface/runs/real-handoff-pick-census-20260924/code-full-batch-proposal.zip`，85 个 Python 文件，SHA-256 `a172bea6c06b0d578cb546835a86d800084e9b93d8e81670ace9b099981691d0`，尚未在实验室执行。它将完整批次授权与前 12 计划摘要 SHA-256 `fae5c93728661f8bbfacd8de8ba6ca6600dba9956939578c4fcbea5cf60f7e4a` 作为硬门，并从位置 12 开始计算**新增** 7200 秒预算。四项 CPU 测试及 120 计划无 GPU dry run 通过。完整批次的实际源码包、回执和费用将于获批执行后再冻结登记。

实验室端已将该候选包、冻结清单和前 12 计划原始回执暂存。只读 CPU 核验通过：85 个源码成员哈希、清单/episode/摘要哈希及 12 份结果与回执逐项相等；下一位置为 12，未发现任何未经授权的完整批次回执。该核验未启动 GPU 或新增样本。

**授权记录**：用户回复“授权”，对应上述实验室 GPU1 最多新增 7200 进程秒、单进程最多 600 秒、API 0 的固定清单续跑。授权文件 `authorization-full-v1.json` 的 SHA-256 为 `22dae7da9fea77205391600404dac2c99c0334598a905e1ec9cd8cfd09c02a52`；本批实际进程时间和结果将另行记录。
