# SAC batch close and restart gate — 2026-09-20 01:14 EDT

Current stage: no-training tool integration and partial baseline evaluation. First batch ended; code fixes passed CPU checks; new twenty episodes are blocked at the existing request-budget gate. Automation `sac` is PAUSED and no further wakeup was created.

## Preserved result and accounting

All 20 seeds finished their latest attempt; after separate audit, 16 are evaluation-terminal and 4 are infrastructure failures (5, 9, 11, 14). There are zero native completed objects and zero complete tasks. Keep planned=20 and 100 object slots; do not report the 16 valid-terminal rows as the original population. The six earlier zero-request infrastructure attempts for seeds 0–2 remain preserved. The original manifest's erroneous `completed=20` is not rewritten.

The 83 distinct remote request IDs exactly match 83 local bridge starts and 83 usage responses. Total usage: 113100 input tokens, 9866 output tokens, 122966 total. The reservation is USD 0.10375, NOT an actual charge. Actual API cost and remaining dollar budget are unknown pending provider reconciliation. There were no new API calls, training, or rented resources during this follow-up. Historical stopped RunPod storage remains an expense; its current bill was not refreshed.

Latest attempts performed 2969 actions and recorded 5439.0016 aggregate episode wall seconds (not total GPU occupancy). All-attempt request consumption is 83, including infrastructure rows. Observed calls per valid-terminal episode are 70/16=4.375; no current episode was stopped by the organizer-call or cost cap. Eleven episodes hit the native navigation horizon instead. This is not a Table 4 comparison. Recovery proxy has no observed intervention/attempt, so its success rate is null. Native same-target success within the bounded window may miss delayed or cross-skill resolution; later Place failure cannot undo an already successful Pick recovery.

## Attribution and changes

1. Environment source contamination: the acdit virtual environment imports `AC-DiT/third_party/mshab`, whose raw extra state adds base angular velocity, linear velocity and position (9 dimensions). Official SAC weights expect 42 dimensions, versus the fork's 51. A CPU reconstruction of the exact loader reproduced the three 50×42 versus 50×51 state-projection mismatches. With 42-dimensional synthetic state and two stacked depth cameras, the real sugar-box SAC checkpoint loads strictly and emits a finite 1×13 action. Historical errors suppressed their messages, so this is a reproduced compatible cause, not a recovered traceback from every failed seed.
2. Runner now puts `/home/pshuai/bvi-research/src/official-mshab-runtime/mshab` ahead of editable imports and checks actual import provenance before episode dispatch. That checkout is `e9ff3d23496d38e4431c8d913e147ffa007f7f72`. Coordinator records the loaded environment path and SHA. No state truncation or weight changes were applied. Real reset/initial-state equivalence and end-to-end manipulation compatibility remain unverified and must pass before rerun.
3. An adapter RuntimeError is now infrastructure failure even when the coordinator exits with code zero. A ProtocolError is not inferred to be model output failure just because earlier API calls occurred. New returned-response violations use `ModelResponseError`; local API budget stops use `APIBudgetExhausted`. For historical seed 4, the `coordinator_rejected` event and JSON `Extra data` traceback confirm invalid returned JSON; this remains a valid failed rollout. No prompt, model or output-repair behavior changed.
4. Eleven 199-action failures had `subtasks_steps_left=0`, `fail=true`, and force still within limit. The pinned native horizon is 200, with reset evaluation consuming one count. This is native horizon behavior; it was not extended or patched.
5. Summaries now count requests from all preserved attempts, report budget-terminated/unknown rows separately, and retain planned denominators. The audit is derived into a new directory; old results remain intact.

Verification: full local CPU suite 626 passed, 6 skipped, one websocket deprecation warning. The remote CPU strict-loader check passed for 42D and deliberately reproduced failure for 51D. No real post-fix episode was run. Fixes are staged locally, not deployed to remote production source.

After the final provenance/denominator additions, 26 targeted regression tests passed again and all three changed runner/audit scripts compiled. Fixed local source bundle: `D:/AI/Embodied Intelligence/VLA as Tools/runs/sac-followup-20260920T0506Z/fixed-local-source.zip`, SHA256 `ea9b2f4ad8d3e23d4bc9ad189f873da90e1dfc3fe22a9fde00b99ac1c10a68a1`.

## Evidence and restart handoff

- [Derived twenty-row audit](results/sac-followup-20260920T0506Z/audit.json), [derived manifest](results/sac-followup-20260920T0506Z/audited-manifest.json), [request reconciliation](results/sac-followup-20260920T0506Z/api-reconciliation.json). Derived manifest is audit-only, not a launch input.
- Original full batch archive: `D:/AI/Embodied Intelligence/VLA as Tools/runs/sac-followup-20260920T0506Z/original-batch.tar.gz`, SHA256 `d842867a38ee68373863b658a72f621e0ba5ca81c5f2d7fbacf917e1b1e41694`. Includes logs, original manifest, all attempts, image/media files and bridge spool.
- Remote source archive, copied locally as `remote-source.tar.gz` in that directory: SHA256 `79df51384cb726eca7b377c9a777f4ffe766e34d73f8c566063a8de6e84275b5`. Remote dirty patch SHA256 `9a12168c5461f6951d9e95db275651b1d08901589493cc77bc050e077b7e2d45`. Local pre-edit src/scripts/tests and dirty.patch are in the same directory. HEAD `67588e3489f0b769326887d56ccbe3050ea75ef7` is not a clean-source claim.
- Remote archive directory: `/home/pshuai/bvi-research/runs/sac-followup-20260920T0506Z`; original run `/home/pshuai/bvi-research/runs/sac-interface-baseline-20260920` remains untouched.

Runner PID 1833185 was already gone. Confirmed no active coordinator/runner before sending SIGTERM to this batch's idle LightNav PID 1827922. Subsequent check: that service is gone, GPU1 UUID `GPU-b7ebba23-7824-7601-df32-be55628936c3` is 15 MiB / 0%, no GPU1 compute process. GPU0 process 672548 was not changed. No local Python bridge process was found. Lab monetary charges remain unknown. LightNav occupied GPU1 from roughly 03:12 through 05:11 UTC (~2 hours, approximate, not exact billing); remaining 18-hour GPU authorization must conservatively account for this interval and earlier smoke before restart.

New batch: planned=20, seeds 0–19 all `not_run`, status `blocked_budget_gate`. 800−83=717 requests remain; twenty episodes at 40 each require 800, a shortfall of 83. Do not reset the ledger or launch a new independent 800-request bridge. Restart needs an explicit allocation decision under the existing cap (or explicit changed authorization), provider cost reconciliation, deployment of the tested changes, verified native reset bindings under the pinned runtime, and a fresh batch/version with links to every old attempt. Restore/reuse the 83 existing bridge claims and request IDs as global consumption. Seeds 20–999 remain unbound/unrun; do not mark them evaluated. No new runner command was executed or PID created.
