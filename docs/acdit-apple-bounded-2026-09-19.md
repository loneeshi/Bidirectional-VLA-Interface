## 正式启动确认：2026-09-19 18:09 UTC

底盘阶段已连续完成20次正式更新，全部损失及梯度有限；权重逐张量保存重读、optimizer状态重读和有限性检查通过，后台训练进程存活。743条唯一成功示范覆盖63场景，按场景分为593训练/150开发，分别26995/7193帧；没有混入失败或开发场景。实际采集1184次、229928次仿真动作/教师调用，此为累计快照，不与先前12840重复相加。

阶段1约20秒/更新，峰值分配8534331904 bytes。第一阶段硬截止2026-09-19约22:01 UTC，正常结束后自动转入本次stage1最佳权重条件下的全身阶段；全身阶段GPU预检已通过，正式训练尚未开始。外层timeout约2026-09-20 10:53:55 UTC（纽约06:53:55）停止，合同截止10:56:28 UTC，无预算扩展。截止用于限制资源，不代表完成论文30k/20k训练量。

一次性s1-ia跟进已经工具确认PAUSED；用户要求的启动确认完成，现在结束聊天监督，不再创建唤醒。没有宣称原生抓取成功、33.3%复现或完整训练已完成。新增租机/API0，lab费用未知，GPU1仍在训练，未声称释放。证据：[launch-verified.json](results/acdit-apple-bounded-2026-09-19/launch-verified.json)。

# Apple Pick 单任务 AC-DiT 有界训练

当前：正式底盘阶段已稳定运行，启动确认完成。用户追加的一次性30分钟跟进已执行并暂停；后续后台两阶段按硬截止运行，不再聊天监督。

## 执行范围

- 实验：`acdit-apple-bounded-20260919-run01`，服务器目录 `/home/pshuai/bvi-research/runs/acdit-apple-bounded-20260919-run01`。
- 原始RDT170M底盘阶段→RDT1B全身阶段；不是旧社区权重上的四组LoRA/进度头训练，不是论文七任务精确复现。
- 第一阶段最多4小时，batch24、LR1e-4；第二阶段使用剩余预算，batch16、LR1e-5；FP32，AdamW，原生两帧视觉、点云、真实速度和privileged context。
- 单次实际运行总上限18小时。VPN中断期间没有训练进程，不计入GPU运行时间；恢复时保守扣除先前900秒，剩余63900秒。精确截止以[运行合同](../configs/acdit_apple_bounded_20260919.json)为准，外层timeout比合同截止更早。
- GPU0已有进程，未操作；只使用GPU1 UUID `GPU-b7ebba23-7824-7601-df32-be55628936c3`。没有RunPod或付费模型API操作。

## 数据门

目标1000条成功示范，采集预算内至少300条唯一成功起点；全部失败保留。使用train任务计划，显式轮换场景及随机初始化，按场景预先分出约20% development，不使用原生test成功率选点。至少10场景、200条训练及30条development轨迹；完整点云、速度、状态必须有限。

首次采集64次得到38条成功，后因非有限教师动作停止。已修正：停止后的环境不再向物理引擎发送NaN；活动环境出现非有限动作时记基础设施失败并排除；不将此事件当作策略成功。另从作者源码确认默认场景构建只取前N个，因此显式传入循环场景名单，避免8环境永久只见8场景。旧资料保留，续采可能重复的起点会在最终数据清单排除。

## GPU预检证据

两阶段均已完成真实前向、反向及Adam一二阶矩显存分配，但没有执行丢弃的optimizer更新。

| 项目 | 底盘阶段 | 全身阶段 |
|---|---:|---:|
| 可训练参数 |162,529,730|1,235,166,402|
| 预检损失 |0.00459140|0.04346583|
| 梯度范数 |0.0458531|0.308407|
| 峰值GPU分配bytes |6,550,820,864|23,925,012,480|
| 权重保存重读逐张量一致 |通过|通过|

预检使用独立的小型shape smoke输入，不作为正式训练数据、开发集分数或原生任务能力证据。正式训练第一个更新还必须验证优化器状态保存，并观察后续更新持续推进。

## 与公开代码的差异

单任务、限时和有效batch缩减已明确标记。保留非EMA模型选择，省略未用于作者评测的EMA副本。第二阶段除加载底盘DiT外，显式转移并冻结第一阶段对应输入投影，避免公开构造器只载DiT、其无梯度输入投影仍随机初始化；同时转移点云编码器参数。这些是具名工程修改，不声称逐字复现论文。

## 无需聊天监督的停止机制

一个立即启动的进程串行完成数据检查、第一阶段、第二阶段。各阶段有硬时间限制，错误退出不自动重试。模型每约15分钟或100更新验证/保存，第一更新也保存；轮换两套latest权重与optimizer/RNG状态，以原子manifest指向完整一对，并另保留best权重。结束或超时后退出、记录GPU快照。该进程没有cron、应用定时任务或自动唤醒。

原生100例或100×3正式评估不包含在此次“确认训练启动后结束监督”的完成声明中。训练损失下降不代表抓取成功。

代码：[采集](../scripts/collect_acdit_rdt.py)、[数据冻结](../scripts/finalize_acdit_rdt_data.py)、[训练](../scripts/train_acdit_rdt_bounded.py)、[顺序执行与限时](../scripts/run_acdit_apple_pipeline.py)。

费用：新增租机/付费模型API为0；实验室费用未知，实际使用写入finance/ledger.json。没有为本次训练刷新RunPod资源或账单。
