# 本周优先级：MS-HAB 上的 VLAs-as-Tools

用户于2026-09-17 UTC澄清：本周主要交付仍是在MS-HAB／ManiSkill中实现VLAs-as-Tools。LIBERO提供参考实现检查，不作为完整论文复现的前置条件。

## 时间边界与当前事实

- LIBERO源码、接口及已有运行证据复核：最多2–4小时，不从零重训。
- 若在实验室重建LIBERO/OpenPI并做小规模回归：工程估计4–8小时，尚非测得时长；依赖安装和权重传输可能超出。2026-09-17本次只读检查在`~/bvi-research`发现acdit/lightnav环境，未发现约定路径下的OpenPI/LIBERO环境及pi05_libero权重。不得据此宣称它们已经部署。
- 完整DROID中间训练、LIBERO目标域适配及全任务评估：不是半天任务。必须先核实作者资产和实测吞吐才能可靠估时，本周不将其设为MS-HAB前置门。

上述时间是工程估算／工作时间上限，不是GPU租用或新增预算授权。

## 执行顺序

1. 复核现有π₀.₅＋LIBERO作者组件：进度头从观测prefix读取、调用内残差固定、语言传递、进度时点、动作队列与事件顺序。复用已有5/5闭环证据，只补确有必要的回归；单任务5/5不升级为论文复现或收益证据。
2. 继续MS-HAB迁移，优先保留π₀.₅／OpenPI的作者结构。核对Fetch观测、实际检查点状态/动作归一化、通道与控制频率；不得复用LIBERO的动作归一化或机械臂动作语义。现有Fetch普通LoRA适配不自动成为TAPT。
3. 接入按调用分段的Fetch工具族残差与作者进度监督，在真实交接数据上做有界TAPT。保持完整父轨迹划分，分开报告接口正确、训练完成、反馈质量和任务成功；不以改阈值代替定位失败原因。
4. 先验证单物体操作链，再接GPT与LightNav，以串行控制权完成导航→抓取→携物导航→放置。LightNav是独立骨干扩展，反馈若非学习预测必须标明来源。
5. 固定初始状态、步数预算和原生成功判定完成小规模对照，保留全部失败记录、准确录像名和逐调用费用。更广任务覆盖、完整DROID流程与GRPO独立安排。

## 暂缓项与报告口径

AC-DiT现有检查点、原始数据、失败记录和CPU配对准备全部保留为迁移对照。暂缓独立观测头的实现／训练，避免自动监督沿用旧路线继续开发；没有删除既有成果。

本周目标表述为“将VLAs-as-Tools／TAPT方法迁移到MS-HAB并验证闭环”。若DROID中间阶段或原文评估规模未完成，明确列为差异；不得称完整论文训练流程或论文成绩复现。保留同一骨干有助于减少变量，并不消除Fetch与论文机器人之间的数据及控制适配。

计算使用实验室GPU1。Runpod不得新租／恢复；实际GPU或API执行前读取资金账本，固定运行上限与收尾保护。本文是优先级协议；实际启动状态见下方记录。

## 2026-09-17 实际启动

作者OpenPI f4eb160已在实验室独立环境安装，CPU导入通过；Fetch V8完整权重正在校验迁移。工作台相机教师检查：Pick29步成功，Place199步失败，均有完整图像/状态/动作记录；固定50父轨迹的采集已开始。该采集是原生起点SAC示范，不是VLA成绩，实际导航交接数据仍需补充。模型接口门与训练状态参见[启动记录](fetch-pi05-author-migration.md)。AC-DiT新头仍暂停。
