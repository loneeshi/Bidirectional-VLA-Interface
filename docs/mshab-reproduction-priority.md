# 本周优先级：MS-HAB 上的 VLAs-as-Tools

用户于2026-09-17 UTC澄清：本周主要交付仍是在MS-HAB／ManiSkill中实现VLAs-as-Tools。LIBERO提供参考实现检查，不作为完整论文复现的前置条件。

## 时间边界与当前事实

- LIBERO源码、接口及已有运行证据复核：最多2–4小时，不从零重训。
- 若在实验室重建LIBERO/OpenPI并做小规模回归：工程估计4–8小时，尚非测得时长；依赖安装和权重传输可能超出。2026-09-17本次只读检查在`~/bvi-research`发现acdit/lightnav环境，未发现约定路径下的OpenPI/LIBERO环境及pi05_libero权重。不得据此宣称它们已经部署。
- 完整DROID中间训练、LIBERO目标域适配及全任务评估：不是半天任务。必须先核实作者资产和实测吞吐才能可靠估时，本周不将其设为MS-HAB前置门。

上述时间是工程估算／工作时间上限，不是GPU租用或新增预算授权。

## 执行顺序（2026-09-17 用户纠偏后）

1. **接口忠实性检查。** 复用作者π₀.₅/OpenPI，保持Fetch V8状态、图像、分位数归一化、动作通道与时点契约；LIBERO只作2–4小时参考审计。严格加载与数值检查不代表控制能力。
2. **新增：冻结V8原生能力门，位于任何TAPT更新之前。** 按9/16环境协议运行`set_table/pick/013_apple`、`val`场景、种子2024–2028，200动作上限，20Hz，不改原生终止/力限制。另报告2025/2030诊断面板；2025与主结果重叠，只新增2030一个episode。5cm是末端回到休息位的阈值，成功还要求持有、机器人归位、静止与累计力条件。分别记录曾抓住、稳定持有、原生成功，保留失败。原AC只保存reset观测，新V8必须匹配qpos及head/hand RGB/depth；不能声称历史全仿真状态完全配对。保留各策略自身动作约定，并披露AC每次执行2动作、V8执行1动作及stationary-head差异。
3. **新增：错误交接验证输入门。** 原生50条保持20/5父轨迹划分和失败保留；它们不能替代错误交接覆盖。固定3020/3021留出父轨迹的远距grasp、未持有move和正常参照，原2025/2030与24.1cm事件作为锁定诊断。历史记录缺workspace视角，必须真实回放、状态校验并重新渲染；任何模型臂使用同一已验证边界输入。训练父轨迹不得混入验证，2025/2030不得用于挑检查点/阈值。失败结束不标完成；调用首帧时钟0不等于物理奖励0。详见[八案例回放计划](results/fetch-pi05-handoff-validation-2026-09-17/plan-v2.json)。
4. **满足以上条件才允许20更新训练检查。** 四族残差选择、原生骨干冻结、作者prefix进度头、有效batch8、恢复点和独立动作/进度mask逐项验证。20更新只验训练通路；不宣称反馈有效。随后才考虑已限定时长的SFT，按独立验证联合损失选检查点。
5. **在线之前增加反馈验收。** 在正常/错误交接输入上报告误完成率、回退/停滞触发和实际恢复结果；分开模型输出与物理判据。保持原阈值，并专门记录BF16阈值附近预测和事件抖动；FP32归因通过不能替代BF16线上验证。然后接GPT及独立LightNav骨干，以串行控制权完成单物体链和固定起点对照。

## 本周明确止损线

- 五个主种子全部按协议完成后，若原生Pick成功 **≤1/5（含0/5）**，**本周停止π₀.₅ TAPT参数训练，包括20更新检查**。基础设施或reset配对失败不记作模型失败，也不能据残缺分母作出成功率结论。
- >1/5仅解除这一项阻塞，不代表TAPT已通过；错误交接输入门、训练门和反馈门仍必须完成。SFT有动作学习目标，不能从0/5推导“训练永远不能改善控制”；该线是本周资源与交付约束。
- 止损后先恢复已有GPT＋SAC单物体闭环及LightNav＋SAC参照，冻结底层控制与协调器设置，单列新增学习进度反馈的对照。SAC不是VLA，给SAC附加进度预测不能称为作者TAPT或VLM＋两种VLA完整复现；它是保交付的工程对照。反馈尚不可靠时先保留影子评估，不以规则接管伪装学习反馈成功。
- DROID完整中间阶段、GRPO和未完成的π₀.₅/LightNav工具对齐继续明确列为缺口，不挤占本周已验证闭环交付。

## 暂缓项与报告口径

AC-DiT现有检查点、原始数据、失败记录和CPU配对准备全部保留为迁移对照。暂缓独立观测头的实现／训练，避免自动监督沿用旧路线继续开发；没有删除既有成果。

本周目标表述为“将VLAs-as-Tools／TAPT方法迁移到MS-HAB并验证闭环”。若DROID中间阶段或原文评估规模未完成，明确列为差异；不得称完整论文训练流程或论文成绩复现。保留同一骨干有助于减少变量，并不消除Fetch与论文机器人之间的数据及控制适配。

计算使用实验室GPU1。Runpod不得新租／恢复；实际GPU或API执行前读取资金账本，固定运行上限与收尾保护。本文是优先级协议；实际启动状态见下方记录。

## 2026-09-17 实际启动

作者OpenPI f4eb160已在实验室独立环境安装，完整Fetch V8权重已校验，71个预训练参数严格加载通过。工作台相机教师检查：Pick29步成功，Place199步失败，均有完整图像/状态/动作记录；固定50父轨迹已完成：Pick21/25、Place18/25成功，全部失败保留。该采集是原生起点SAC示范，不是VLA成绩，实际导航交接数据仍需补充。模型接口门与训练状态参见[启动记录](fetch-pi05-author-migration.md)。AC-DiT新头仍暂停。

## 当前执行状态：训练已冻结

用户提出新门后，run03自动控制器已定向停止，参数更新仍为0；已有固定50条采集独立完成后不会自动训练。远端训练hold、训练CLI门和监督提示共同阻止旧顺序恢复。冻结V8原生run01在0动作时因客户端模块路径失败，修复后run02已实际执行固定评估；错误交接回放输入正在准备；这些阶段均无GPT调用或新租GPU。


## 止损线已触发：冻结 V8 主结果 0/5

2026-09-17 run02 已按固定 val 种子2024–2028完成全部主测试，成功0/5；五条均未出现原生 grasp，分别在20/24/36/25/37步触发累计接触力上限。每条起点qpos和head/hand RGB/depth与历史记录严格一致。没有修改模型、提示词、动作mask、节奏或阈值。本周π₀.₅ TAPT参数训练停止，包括20更新；远端hold已升级为STOP_PI05_TAPT_THIS_WEEK。辅助2030与错误交接回放仅补证据，不能用于反转主结果或解除训练止损。


## 错误交接数据门已完成；训练门保持停止

8个固定案例全部通过：5份历史父轨迹各独立重建两次，初始simulator/controller/旧相机严格一致，逐步状态、原生反馈与奖励核对；新增workspace RGB来自实际渲染。4个3020/3021留出验证输入＋4个锁定历史诊断输入已归档。保持父轨迹划分，不把失败片段结束标为完成。详见[回放输入与边界](results/fetch-pi05-handoff-replay-2026-09-17-run01/README.md)。它们能支持后续误完成与回退评估，尚不表示进度模型已通过这些测试。

[完整原生结果与止损记录](fetch-v8-native-capability.md)。所有GPU实验已结束并复查GPU1为15MiB/0%；完整教师3.93GB归档、241文件原生归档和19文件回放归档均已本地哈希验证。下一步是固定现有GPT＋SAC控制基线与独立LightNav＋SAC对照，先验证学习反馈，不恢复本周π₀.₅训练。
