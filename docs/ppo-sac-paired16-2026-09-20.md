# Paired sixteen-plan study

User authorized 16 episodes each: official fixed task-order PPO navigation + per-object SAC, versus GPT-5.6 Luna execution organizer using identical executors. No training. Not a replication of the paper's teleport-navigation score.

Selection: first occurrence of each of the 16 unique plan UIDs among previous seeds0–19; seeds0–11,13,14,16,19. Paired resets must match the simulator state SHA before any GPT request. Each episode is five objects, twenty native subtasks. Oracle plan order and target admissibility are retained in both arms; GPT does not freely reorder objects.

Official MS-HAB e9ff3d runtime: navigation horizon500, pick/place200. Earlier AC-DiT fork used navigation200 and state51; these are incompatible historical results, preserved separately. Each call40 actions/180s; episode7000 actions/900s. Fixed scheduler may continue up to175 slices, GPT has40 paid decisions, output2048 tokens maximum, USD0.05 reservation cap/episode. GPT additionally receives grasp-event interruptions and can abort; this is a combined organizer/feedback comparison, not isolated feedback ablation. Both arms see the same declared camera-equipped environment; PPO/SAC use native depth/state observations and do not consume language.

Authorization: continue prior SAC scope. 83 existing requests plus at most640 new =723, below original800. Reuse original authorization ID and local bridge claim ledger, never reset consumption. Existing USD2 total cap retained; USD0.10375 previously reserved plus at mostUSD0.8 newly reserved. Actual provider charges unknown, not zero; reservations are not a provider bill. GPU1 only, no RunPod, GPU0 untouched. Outer panel budget33000s (<9.2h), plus bounded compatibility check; total new work remains within18h allowing prior ~2h occupancy. Finish deadline September21 unchanged.

Output root: /home/pshuai/bvi-research/runs/ppo-sac-paired16-20260920. New immutable source bundle, old runs untouched. Interleave fixed/GPT for each plan; stop on first infrastructure failure and retain all32 rows. Native failure is a measured result, not infrastructure. Resume only after verifying no live runner/child; preserved running attempts become interrupted and a new attempt directory is used. Stale runner.lock requires verified PID reconciliation before archival/removal.

Record SR with planned denominator16 per arm, completed/infrastructure/not_run counts, completed objects0–5, per-skill progression, all-attempt requests, wall/actions and cap termination. Early failures do not reduce the planned denominator. Execution speed and ETA to be measured during startup; 32×900s gives8h of episode caps plus startup/teardown, not a prediction of typical runtime.
