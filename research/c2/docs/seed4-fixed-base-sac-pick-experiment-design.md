# seed4 固定底盘 SAC Pick：运行前设计

按[实验日志模板](../../../docs/log/TEMPLATE.md)设计**一项单独的对照诊断**。本页冻结后才可运行；它不属于已结束的 seed4 Oracle 共驾批次，不能使用该批未花的 433.579 GPU1 秒。

## 实验 #—（固定底盘可抓性诊断，待授权）

- **日期**：2026-09-24 UTC（设计）
- **要回答的问题**：从 seed4 同一导航→Pick 交接起点开始，在底盘前进与转向命令全程均为物理零速的情况下，官方 SAC 只控制其余 11 个动作维度，能否在同一官方 episode 内严格抓住桌上的 `024_bowl-0`？若不能，是手臂无法靠近、非目标碰撞、还是 39 步时限先到？
- **run 名称 / ID**：`seed4-fixed-base-sac-pick-20260924`。
- **代码版本**：本地项目基准 commit `dfa7aa885c60740b834e81d796a9dd4473d768a8`；复用 seed4 Oracle 诊断已核对的 #015 v3 官方 MS-HAB `sequential_task.py` SHA-256 `47fd4999f7326568a550890caa48eac070901525373fac667ff56384eeb2a6be`；新运行器 SHA-256 `c3db7980c4a8fe748c2a0533c53e9639f5744cd84c7816af8f5ee902576b74e3`，零底盘动作模块 SHA-256 `f9d343d82b1737d5dc226cd32d314ebb01762307dbd1211a8b5245c20e002674`，冻结代码包 SHA-256 `236ab1cd129063d4a2b99f6a8bbb9869ed81d5047243c1a0f3f2acee009f5858`，同处理交接提案 SHA-256 `fa72808f515c3f5265bb1b4eef99c135f5db4b86eddb6f0a04ac1756f94d5792`。不得改写先前 Oracle 轨迹。
- **环境**：TidyHouse validation、官方逐物体 SAC；实验室 GPU1 Quadro RTX 8000。GPU0、RunPod、训练资源不用。

### 配置（评测参数）

| 项 | 值 |
|---|---|
| 任务 & 数据划分 | seed4 历史 Pick 交接，目标 `024_bowl-0`，位于 `table_01-5`；复用同一 346 步前缀、原样物理状态写回与 1 帧稳定动作。 |
| plan roster / episode 数 | 一个固定底盘 SAC episode；仅在仿真前基础设施失败时允许同协议重试一次，失败回执保留。对照引用已完成的 #015 无干预基线，不改写其结果。 |
| 导航策略 | 无 Pick 内 PPO/Navigate；不移动起点，不重置场景。 |
| Pick / Place 策略 | 每步调用同一官方 SAC 生成完整 13 维动作，仅将底盘前进和转向两维转换为物理 `0 m/s, 0 rad/s`；保留其余 11 维原值，尤其手臂和夹爪。Place 不适用。 |
| 调度器 / 通讯协议 | 从 Pick 第 0 步开始执行到官方成功、官方失败或 39 步时限；无 GPT、无中途人为改向。零速通过原生动作接口执行，不瞬移、不清零初始速度；逐步记录实际底盘位置/朝向漂移。 |
| observation / policy state | 官方 SAC 42 维及相机输入保持不变；记录原始 SAC 底盘建议、实际执行底盘命令、头/腕相机、夹爪到碗距离、目标接触力、机器人受力、抓持及官方状态。 |
| skill horizon / episode 上限 | 原交接剩余 39 个 Pick 动作、继承累计力约 546.990 N、官方累计力上限 5000 N；不增加动作预算。 |
| 模型调用与费用边界 | **待用户单独授权**：实验室 GPU1 累计最多 450 个进程墙钟秒，每进程最多 225 秒；付费模型请求 0、API 预留 USD 0；实验室货币单价、币种及最高货币金额当前未知，明确保留未知。旧批余额不转用。 |
| 随机种子 | seed4；固定同处理快照，核对目标、任务指针、剩余步数及初态哈希。 |
| 其它关键设置 | 两个底盘维度都必须为物理零速；不得把原运行器的 `brake_base` 误当成固定底盘。底盘相对交接位姿的最大平移须 ≤0.05 m，最大航向漂移须 ≤0.05 rad，才将此轨迹标为“固定底盘验收通过”；若超过阈值或仍碰撞，继续取得官方结局并按实际情况记录，不把它冒充成功固定。 |

### 结果（运行前留空）

| 指标 | 值 | 备注 |
|---|---|---|
| 完成 episode | 待运行 | 仅一个 seed、一个条件。 |
| 官方严格 Pick | 待运行 | 主指标。 |
| 曾抓持 / 碗接触 | 待运行 | 辅助指标。 |
| 夹爪到碗最近距离 | 待运行 | 检查“再伸一点”是否成立。 |
| 底盘最大平移/转向漂移 | 待运行 | 核对真正静止的程度。 |
| 非目标碰撞、累计力、终止原因 | 待运行 | 与官方终止记录对齐。 |
| 环境步数 / GPU1 秒 / API 请求 / 费用 | 待运行 | 按逐进程回执入账，未知金额不写成零。 |

### 观察与结论（运行前留空）

- **观察**：待运行。
- **异常**：待运行。
- **结论**：单 seed 只回答这个交接点的固定底盘可抓性，不作总体成功率推断。

### 停止规则与下一验收门

- 未取得上述新额度授权前，不启动 GPU1 仿真。
- 若启动时哈希、起点身份、官方运行时、底盘零速映射有误，仿真前停止并保留回执；不算 Pick 结果。
- 官方 Pick 成功或失败即停止；不因看到局部进展延长时限。
- 若固定底盘严格失败，先依据最小夹爪距离、接触和漂移判断瓶颈；只有需要时再单独设计不同起点或导航交接实验。
