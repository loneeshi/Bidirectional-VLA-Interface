# seed4 同起点固定底盘延长 Pick 时限：运行前设计

按[实验日志模板](../../../docs/log/TEMPLATE.md)设计**一项独立诊断**。用户选择保留[上一项固定底盘实验](../diagnostics/2026-09-25-fixed-base-pick/seed4-fixed-base-sac-pick.md)的同一交接状态，只把当时剩余 Pick 动作从 39 补到 200；不把上一批未用的 GPU1 秒数转进本批。修改任务计时后，任何成功只属于**延长时限条件**，不记为原始 benchmark 的严格 Pick 成功。

## 实验 #—（同起点延长时限，待授权）

- **日期**：2026-09-25 UTC（设计）。
- **要回答的问题**：在 seed4 同一个起点、同一碗、底盘固定、SAC 手臂/夹爪不变的情况下，将剩余 Pick 动作从 39 增至 200，能否碰到碗、抓住碗、满足官方 Pick 成功和受力条件？若仍失败，夹爪距离是否改善？
- **run 名称 / ID**：`seed4-fixed-base-extended-horizon-20260925`。
- **代码版本**：项目基准 commit `dfa7aa885c60740b834e81d796a9dd4473d768a8`；新时限包装器 SHA-256 `771e066d9066058d9eb92ed92a9dfadab92f380f1f31a46dff6cab1e2f80b4c1`，冻结代码包 SHA-256 `58df440b277c4db1c7759cbefd8fef25de2e3aad0b4eac9b926ee323f8eb7eef`，交接提案 SHA-256 `fa72808f515c3f5265bb1b4eef99c135f5db4b86eddb6f0a04ac1756f94d5792`。官方 MS-HAB `sequential_task.py` 不修改，SHA-256 `47fd4999f7326568a550890caa48eac070901525373fac667ff56384eeb2a6be`。
- **环境**：TidyHouse validation、官方逐物体 SAC、实验室 GPU1 Quadro RTX 8000；沿用已验证的 Python 3.11.13、PyTorch 2.7.0+cu128、ManiSkill 3.0.0b18。GPU0、RunPod、训练资源不用。

### 配置（评测参数）

| 项 | 值 |
|---|---|
| 任务 & 数据划分 | seed4 目标桌上碗 `024_bowl-0`；重放相同历史前缀 346 步，原样物理写回与 1 帧稳定动作，核对进入时任务指针、位置/朝向、物体、速度、累计力和剩余 39 步。 |
| plan roster / episode 数 | 一条延长时限的固定底盘轨迹；只在仿真前基础设施失败时同协议重试一次。原始 39 步固定底盘轨迹保留作配对参照。 |
| 导航策略 | 无 Pick 内 Navigate/PPO；起点、物体及底盘物理状态不改。 |
| Pick / Place 策略 | 每步调用原官方 SAC 生成 13 维动作，保留前 11 维，仅将底盘前进/转向两维设为物理 `0 m/s, 0 rad/s`。Place 不适用。 |
| 调度器 / 通讯协议 | 同处理交接验证后，**仅**将环境内部 `subtask_steps_left` 从 39 写为 200，并确认适配器读到 200；不改 `elapsed_steps`、SAC、总累计力、目标、任务指针或物理状态。运行到该条件下的任务成功、失败或 200 Pick 动作；不再中途改计数。 |
| observation / policy state | 官方 SAC 42 维及相机不变；保存每步原始/实际动作、夹爪到碗距离、抓持、碗接触、机器人受力、底盘漂移及任务计数。 |
| skill horizon / episode 上限 | 原始官方 Pick 分段上限为 200；该历史快照已经用过 161 步，所以此前只余 39。此次从**相同快照**补到剩余 200，属于时限反事实；环境每步扣 1。累计力仍从原有约 546.990 N 起算、官方力上限 5000 N。 |
| 模型调用与费用边界 | **待单独授权**：实验室 GPU1 累计 ≤600 进程墙钟秒，单进程 ≤450 秒；付费模型/API 0。实验室单价、币种及最高货币金额未知，不把未知写成零。上批余额不转用。 |
| 随机种子 | seed4、相同运行时、相同交接提案和 checkpoint。 |
| 其它关键设置 | 底盘最大平移漂移 ≤0.05 m、最大航向漂移 ≤0.05 rad 才验收为固定底盘；逐步对照前 39 个实际动作/状态与上一轨迹，报告首次分歧。 |

CPU 预检已经完成：新脚本语法解析通过；13 维动作的前 11 维保留、后两维物理零命令映射通过；代码包有 83 个不重复路径且内含新脚本的逐字节副本。**尚未运行仿真或付费 API**。预检回执位于 `Bidirectional-VLA-Interface/runs/seed4-fixed-base-extended-horizon-20260925/cpu-preflight.json`。

### 结果（运行前留空）

| 指标 | 值 | 备注 |
|---|---|---|
| 延长时限下 Pick 成功 | 待运行 | 同时列环境成功、失败、抓持及累计力。**不作原始 benchmark 成功率**。 |
| 第一次碗接触 / 抓持步数 | 待运行 | 若始终没有，明确记为无。 |
| 夹爪到碗最近距离及发生步数 | 待运行 | 与 39 步条件的第 0 步 1.418862 m 比较。 |
| 前 39 步一致性 | 待运行 | 核对改计数是否扰动动作/物理轨迹。 |
| 最大底盘漂移 / 碰撞受力 | 待运行 | 判定固定底盘控制有效。 |
| 有效动作 / GPU1 秒 / API / 货币费用 | 待运行 | 逐进程入账；未知实验室金额保持未知。 |

### 观察与结论（运行前留空）

- **观察**：待运行。
- **异常**：待运行。
- **结论**：只回答“同一状态多给时间”这一反事实，不把修改时限的结果混入原始官方评分。

### 停止规则与下一验收门

- 未取得该批 GPU1 时长/未知货币费用的单独授权前，不启动仿真。
- 起点身份、源码哈希、零底盘动作映射或计数写入不符时，仿真前停止并保留回执。
- 一条轨迹完成官方终止即结束；不因局部进度增加第三次计数或切换控制器。
- 若延时仍无目标接触，下一验收门是只读几何可达性分析或另行设计真正 Pick 起点，不在本批中试多个起点。
