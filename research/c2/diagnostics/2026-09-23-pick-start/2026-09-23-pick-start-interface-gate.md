# Pick 第 0 步监督接口验收

## 实验 #009：第 0 步检查与真实续跑接口验收

- **日期**：2026-09-23 至 2026-09-24 UTC
- **运行前设计**：[冻结协议的实验 #009 节](../../docs/pick-start-supervision-recovery-experiment-design.md)；原设计保留作时间顺序证据
- **要回答的问题**：Pick 第一个 SAC 动作前能否实际调用 GPT；`continue` 是否不扰动轨迹；当前真实状态的移动能否到达并用新观测续跑？
- **run 名称 / ID**：`pick-start-supervision-recovery-20260923/run-v4/independent/G1-P2-*`；只用一个起点（场景/任务组 G1 的第 2 个预定站位，距目标 1.4 米、方位角 +30°），五条轨迹各自重建；不计入 #011 效果比较
- **代码版本**：执行仓库 HEAD `dfa7aa885c60740b834e81d796a9dd4473d768a8` 加本批未提交执行文件，冻结源码包 SHA-256 `f2dbc8542c22df92e59c2d3ae4f4a895f589f8e8172574612b93b9363827804d`；MS-HAB `e9ff3d23496d38e4431c8d913e147ffa007f7f72`，ManiSkill `17121e3f96e3ee3ed0c03610b17f8bc2864617af`
- **环境**：Python 3.11.13 / ManiSkill 3.0.0b18 / PyTorch 2.7.0+cu128 / 实验室 GPU1

### 配置（评测参数）

| 项 | 值 |
|---|---|
| 任务 & 数据划分 | 官方 TidyHouse validation 单物体 Pick |
| plan roster / episode 数 | `G1-P2` 无干预、纯检查、偏航移动、平移移动、真实 GPT 试运行各独立重建；均排除于 #011 比较 |
| 导航策略 | 离散候选的当前状态路径检查；控制器实际移动，逐动作测到达 |
| Pick / Place 策略 | 同一官方逐物体 SAC；Place 不适用 |
| 调度器 / 通讯协议 | 纯检查第 0 步返回 `continue`；真实试运行第 0 步先调用 GPT，再按固定频次检查；最多三次 GPT 检查、一次移动 |
| observation / policy state | GPT 当前头部/腕部相机及当时状态和可行候选；SAC 原始深度历史及官方 42D 状态 |
| skill horizon / episode 上限 | 官方 Pick 200 动作、原累计力门；移动动作计入同一 episode |
| 模型调用与费用边界 | 本接口 GPT 试运行 2 次物理请求，预留 USD 0.05；实际供应商费用未知 |
| 随机种子 | 冻结 reset seed 和起点；各组独立重建 |
| 其它关键设置 | 纯检查逐 native action/info 与基线一致；偏航和平移均实际到达并续跑；GPT 第 0 步请求、有效回答、决策先于第一条 SAC 动作 |

### 结果

| 指标 | 值（均值 ± population SD，N episodes） | 备注 |
|---|---|---|
| 完成 episode | 5/5 条运行 | 同一起点，五种接口条件；不是五个独立样本 |
| 完整任务 SR | 不适用 | 单物体 Pick |
| 严格 Pick / 完成物体 | 5/5 | GPT 试运行选 C5、移动到达；基线也成功，非救回 |
| 每集完成物体 | 1.0 ± 0.0，N=5 | 仅当前 Pick |
| 环境步数 / 时延 | 44.4 ± 12.55 动作，N=5；GPU1 进程墙钟 87.040 秒 | 五种接口条件的描述性汇总；GPT 试运行含移动 20 步 |
| API 请求 / 费用状态 | 2 次，797 输入 / 95 输出 token | 预留 USD 0.05，实际费用待账单 |

| 同一起点的轨迹 | 检查方式 | 原生动作数 | GPT 实际请求 | 是否真实调整到达 | 官方严格 Pick |
|---|---|---:|---:|---|---|
| G1-P2-baseline | 无干预 | 30 | 0 | 无调整 | 成功 |
| G1-P2-check-start-only | 第 0 步纯检查并继续 | 30 | 0 | 无调整 | 成功；与基线逐动作/info 一致 |
| G1-P2-move-start-smoke | 确定性偏航接口 | 50 | 0 | 到达并续跑 | 成功 |
| G1-P2-move-translation-smoke | 确定性平移接口 | 62 | 0 | 到达并续跑 | 成功 |
| G1-P2-periodic_start | 真实 GPT 第 0 步起监督 | 50 | 2 | 选 C5，移动 20 步到达并续跑 | 成功 |

### 观察与结论

- **观察**：第 0 步真实 GPT 暴露和纯检查不扰动轨迹的门均通过；平移、偏航控制器的到达与严格评分接口通过。
- **异常**：无须把试运行样本计入恢复效果；它的无干预基线已成功。
- **结论**：本批正式比较可使用已验收的第 0 步调用与同 episode 续跑接口。

### 下一步 / TODO

- 无。

---

## 负结果备忘

| 试了什么 | 结果 | 可能原因 / 避免重复方式 |
|---|---|---|
| 第 0 步纯检查与真实移动 | 接口门通过，未发现负结果 | 此项只验接口，不计算效果 |
